// Forward exports so the canonical import path is `@/shared/components`.
// The underlying files will be physically moved during Phase B.
export { BackButton } from "@/components/shared/BackButton";
export { IconTile } from "@/components/shared/IconTile";
export { PageHeader } from "@/components/shared/PageHeader";
export { SectionHeader } from "@/components/shared/SectionHeader";